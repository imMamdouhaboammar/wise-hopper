# الكتل البرمجية واللغات المتعددة

كتلة برمجية بلغة TypeScript:

```typescript
export interface ArticleContent {
  readonly id: string;
  readonly title: string;
  readonly readingTimeMinutes: number;
}
```

كتلة برمجية بلغة CSS:

```css
.editorial-container {
  max-inline-size: 68ch;
  margin-inline: auto;
  line-height: 1.85;
}
```
